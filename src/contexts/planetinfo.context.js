// import { createContext } from "react"

// export const PlanetInfoContext = createContext({
//     //actual value you want to access

// });

// //the actual component
// export const PlanetInfoProvider = ({ children }) => {
//     //the component that wraps around the components that need that info
//     return <PlanetInfoContext.Provider value={ }>{children}</PlanetInfoContext.Provider>
// }